# Piper WASM files

Place the following files from https://github.com/ken107/piper-browser-extension here:

- `piper-worker.js`  — the compiled WASM worker bundle

These files are ~2MB and are not committed to this repo due to size.
Run `npm run build` from that repo or download the release artifacts.
